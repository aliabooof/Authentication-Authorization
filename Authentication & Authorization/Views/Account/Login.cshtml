@using Authentication___Authorization.ViewModels
@model LoginViewModel
@{
    ViewData["Title"] = "Login";
    Layout = "~/Views/Shared/_Account.cshtml";
}

<h1>Login</h1>

<div class="bg-white p-8 rounded-lg shadow-lg w-full max-w-md">
    <h2 class="text-2xl font-semibold text-center text-gray-700 mb-6">Login to Your Account</h2>

    <form asp-action="Login" method="POST">
        <div class="mb-4">
            <label for="email" class="block text-sm font-medium text-gray-600">Email Address</label>
            <input type="email" asp-for="Email" class="mt-2 p-2 w-full border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500">
            <span asp-validation-for="Email" class="text-red-400 text-"></span>
        </div>

        <div class="mb-4">
            <label for="password" class="block text-sm font-medium text-gray-600">Password</label>
            <input type="password" asp-for="Password" class="mt-2 p-2 w-full border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500">
            <span asp-validation-for="Password" class="text-red-400 text-"></span>
        </div>

        <div class="mb-6 flex items-center justify-between">
            <label class="inline-flex items-center text-sm text-gray-600">
                <input type="checkbox" asp-for="RememberMe" class="form-checkbox text-blue-600">
                <span class="ml-2">Remember me</span>
            </label>
            <a href="#" class="text-sm text-blue-500">Forgot Password?</a>
        </div>

        <button type="submit" class="w-full bg-blue-500 text-white py-2 rounded-md hover:bg-blue-600 focus:outline-none focus:ring-2 focus:ring-blue-500">Login</button>
    </form>

    <p class="mt-4 text-center text-sm text-gray-600">
        Don't have an account? <a href="#" class="text-blue-500">Sign up here</a>
    </p>
</div>
